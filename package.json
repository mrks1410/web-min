{
  "name": "web-min",
  "version": "1.0.0",
  "description": "minimum web server for test.",
  "main": "index.js",
  "scripts": {
    "start": "http-server -a",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "http-server": "^0.12.3"
  }
}
